# App BiT — Backend API Dependencies
# Monorepo: /backend
# Stack: Python 3.11+ | FastAPI | SQLModel | PostgreSQL

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM + database
sqlmodel==0.0.22
psycopg2-binary==2.9.10

# Data validation (bundled with FastAPI, pinned for reproducibility)
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.3.0

# Geographic validation (ISO 3166-1 / 3166-2 cross-reference)
pycountry==24.6.1

# Phone validation (E.164 — Google libphonenumber)
phonenumbers==8.13.48

# Authentication (JWT)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.18

# HTTP client (para llamadas a servicios externos / IA)
httpx==0.28.1

# Groq — LLM para el agente de bienestar (IA)
groq>=0.18.0

# CORS
# (FastAPI lo maneja vía CORSMiddleware — sin dependencia extra)

# Testing
pytest>=8.0.0
